<?xml version="1.0"?>
<ruleset name="SymfonyProject">
  <description>PSR-12, no underscore madness, docblocks optionnels, ligne 120.</description>

  <!-- Standard de base -->
  <rule ref="PSR12"/>

  <!-- Exclusions de répertoires -->
  <exclude-pattern>vendor/*</exclude-pattern>
  <exclude-pattern>var/*</exclude-pattern>
  <exclude-pattern>public/bundles/*</exclude-pattern>
  <exclude-pattern>node_modules/*</exclude-pattern>
  <exclude-pattern>migrations/*</exclude-pattern>

  <!-- Longueur de ligne (85 c'est trop court) -->
  <rule ref="Generic.Files.LineLength">
    <properties>
      <property name="lineLimit" value="120"/>
      <property name="absoluteLineLimit" value="0"/>
      <property name="ignoreComments" value="true"/>
    </properties>
    <!-- Tu peux changer en 'warning' si tu veux être soft -->
    <type>warning</type>
  </rule>

  <!-- Pas d'obligation de docblock partout -->
  <exclude name="Squiz.Commenting.FileComment"/>
  <exclude name="Squiz.Commenting.FunctionComment"/>

  <!-- Vire les règles héritées style PEAR qui imposent _propriété privée -->
  <exclude name="PEAR.NamingConventions.ValidVariableName.PrivateNoUnderscore"/>
  <exclude name="Zend.NamingConventions.ValidVariableName.PrivateNoUnderscore"/>
  <exclude name="Squiz.NamingConventions.ValidVariableName.PrivateNoUnderscore"/>

  <!-- Tweaks utiles -->
  <config name="encoding" value="utf-8"/>
  <config name="tab_width" value="4"/>
</ruleset>
