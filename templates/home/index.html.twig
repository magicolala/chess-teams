{% extends 'base.html.twig' %}

{% block title %}Accueil – Chess Teams
{% endblock %}

{% block body %}
	<h1>Bienvenue 👋</h1>

	<section style="margin:1rem 0">
		<form method="post">
			<label>Durée par tour (sec)
				<input type="number" name="turnDuration" value="60" min="10" max="600">
			</label>
			<label>Visibilité
				<select name="visibility">
					<option value="private">Privée</option>
					<option value="public">Publique</option>
				</select>
			</label>
			<button type="submit">Créer une partie</button>
		</form>
	</section>

	<section style="margin:1rem 0">
		<form method="get" action="{{ path('app_game_show_page') }}">
			<label>Code d’invitation
				<input type="text" name="code" placeholder="ABCD12" required>
			</label>
			<button type="submit">Rejoindre</button>
		</form>
	</section>
{% endblock %}
