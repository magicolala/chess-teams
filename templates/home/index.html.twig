{% extends 'base.html.twig' %}

{% block title %}Accueil – Chess Teams
{% endblock %}

{% block body %}
	<div class="container container-sm">
		<h1>Bienvenue 👋</h1>

		<section class="section">
			<div class="card">
				<div class="card-header">
					<h3 class="mb-0">Créer une partie</h3>
				</div>
				<div class="card-body">
					<form method="post" action="{{ path('app_game_create_form') }}" class="form-row">
						<div class="form-col">
							<label for="turnDuration" class="form-label">Durée par tour (sec)</label>
							<input type="number" id="turnDuration" name="turnDuration" value="60" min="10" max="600" class="form-control">
						</div>
						<div class="form-col">
							<label for="visibility" class="form-label">Visibilité</label>
							<select id="visibility" name="visibility" class="form-control">
								<option value="private">Privée</option>
								<option value="public">Publique</option>
							</select>
						</div>
						<div class="form-col-auto">
							<button type="submit" class="btn btn-primary btn-lg">Créer une partie</button>
						</div>
					</form>
				</div>
			</div>
		</section>

		<section class="section">
			<div class="card">
				<div class="card-header">
					<h3 class="mb-0">Rejoindre une partie</h3>
				</div>
				<div class="card-body">
					<form method="get" action="{{ path('app_game_show_page') }}" class="form-row">
						<div class="form-col">
							<label for="invitationCode" class="form-label">Code d'invitation</label>
							<input type="text" id="invitationCode" name="code" placeholder="ABCD12" required class="form-control">
						</div>
						<div class="form-col-auto">
							<button type="submit" class="btn btn-primary btn-lg">Rejoindre</button>
						</div>
					</form>
				</div>
			</div>
		</section>
	</div>
{% endblock %}
